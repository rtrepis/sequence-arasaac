// Constants per als valors per defecte dels ajustos globals del pictograma (uiSlice defaultSettings)

// --- ARASAAC API ---
export const DEFAULT_SKIN = "white" as const;
export const DEFAULT_HAIR = "brown" as const;
export const DEFAULT_FITZGERALD = "#666666";
export const DEFAULT_COLOR = true;

// --- Font de text ---
export const DEFAULT_FONT_FAMILY = "Roboto" as const;
export const DEFAULT_FONT_SIZE = 1;
export const DEFAULT_FONT_COLOR = "#000000";

// --- Vora interior (borderIn) ---
export const DEFAULT_BORDER_IN_COLOR = "fitzgerald";
export const DEFAULT_BORDER_IN_RADIUS = 20;
export const DEFAULT_BORDER_IN_SIZE = 2;

// --- Vora exterior (borderOut) ---
export const DEFAULT_BORDER_OUT_COLOR = "#999999";
export const DEFAULT_BORDER_OUT_RADIUS = 20;
export const DEFAULT_BORDER_OUT_SIZE = 2;
