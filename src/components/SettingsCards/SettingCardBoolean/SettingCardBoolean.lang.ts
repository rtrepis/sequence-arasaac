import { defineMessages } from "react-intl";

export const messages = defineMessages({
  numbered: {
    id: "components.settingCard.numbered.title",
    defaultMessage: "Numbered",
    description: "Setting title",
  },
});
