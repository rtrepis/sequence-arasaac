import { defineMessages } from "react-intl";

const messages = defineMessages({
  load: {
    id: "components.buttonWithFileLoad.load.label",
    defaultMessage: "Load",
    description: "helper text",
  },
});

export default messages;
