import { defineMessages } from "react-intl";

const messages = defineMessages({
  menuAriaLabel: {
    id: "components.logoMenu.menuAriaLabel",
    defaultMessage: "Main menu",
    description: "Aria label for the logo menu button",
  },
});

export default messages;
