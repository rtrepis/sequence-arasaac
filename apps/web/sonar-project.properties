sonar.projectKey=rtrepis_sequence-arasaac
sonar.organization=rtrepis

sonar.sources=./src

sonar.test.inclusions=src/**/*.test.ts

sonar.javascript.lcov.reportPaths=coverage/lcov.info

sonar.coverage.exclusions=src/index.tsx, src/reportWebVitals.ts, src/**/*.test.ts, src/**/*.test.tsx, src/setupTests.ts, src/app/store.ts

sonar.scm.provider=git

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=sequence-arasaac
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
